EXPERIMENT - 5

COPY ELEMENTS OF ONE ARRRAY INTO ANOTHER ARRAY

CODE:

#include <stdio.h>

int main() {
    int original[100], copy[100], n, i;

    printf("Enter number of elements: ");
    scanf("%d", &n);

    printf("Enter elements:\n");
    for(i = 0; i < n; i++) {
        scanf("%d", &original[i]);
    }

    // Copying elements
    for(i = 0; i < n; i++) {
        copy[i] = original[i];
    }

    // Display copied array
    printf("Copied array:\n");
    for(i = 0; i < n; i++) {
        printf("%d ", copy[i]);
    }

    return 0;
}
